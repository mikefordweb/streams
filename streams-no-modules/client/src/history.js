// the history package was installed automatically with 
// 'react-router-dom', as a dependency of 'react-router-dom'
// below we are calling createHistory, a function we can call 
// to create a new history object
import createHistory from 'history/createBrowserHistory';

export default createHistory();